package chap01;

public class RunStatemenExample {

	public static void main(String[] args) {
//		int x =1;     x = 변수 / 1 = 상수
		int x =5;
		int y =2;
		int result = x + y;
		System.out.println(x + "+" + y + "=" + result);
		

		
/* 
 * int같은 형태를 지정하는것은 데이터 낭비를 막기 위한것
 * 형태 지정 첫 시작문 대문자 일때는 클래스
 * [] 대괄호 : 배열에쓰임
 * {} 중괄호 : 묶음에쓰임
 * () 소괄호 : 함수에쓰임
 * 
 * Class : 변수와 함수(펑션)의 묶음
 * Method : 클래스의 멤버 함수 (클래스를 사람으로 표현한다 가정했을때 메소드는 기능을 말함 말하기 달리기)
 * field, Property : 클래스의 멤버 변수 -> 속성
 * Static(정적변수) : 전체제 영향을 주지만 특정구간에서만 사용가능
 * 
 * 
 * *조건문
 *  -Jump : if(조건)~
 *  -Switch : if(조건)~else
 *  -Multi-Select(다중선택) : 1) if(조건) ~elseif(조건) ~ elseif(조건) ~else
 *  							ex) a==5, c==8
 *  					   2) switch(조건) ~case~(defalut)
*  							    ex) a==5, a==8  -> 같은 변수에 대해
*  
 * *반복문
 *  -갯수 : for(초기값; 조건; 증가)
 *  -조건 : while(조건)~, do~while(조건);
 *  
 *  *플로우 차트 3종류 : 순차, 선택, 반복
 *  
 *  * 파일의 종류
 *   1. 텍스트 파일
 *   2. 이진파일(이미지,음악,동영상 등)
 *   
 *  * DOS 명령어
 *  - dir, dir/w, dir/p : 목록 확인
 *  - cd(ChangeDirectory) : 디렉토리 변경
 *  	. 현재위치
 *  	.. 이전위치
 *  	\ 루트 디렉토리(최상위를 말함(ex D:)) 최상위로 감
 *  - type 파일 내용 확인
 *  
 *  
 *  
 *  
 *  
 */
	}

}
